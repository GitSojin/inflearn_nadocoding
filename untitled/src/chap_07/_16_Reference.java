package chap_07;

public class _16_Reference {
    public static void main(String[] args) {
        // 참조

        // 기본 자료형 : int, float, double, long, boolean ...
        int[] i = new int[3];
        System.out.println(i[0]);   //0
        // 기본 자료형은 따로 값을 지정하지 않을 경우 0이나 0.0과 같이 기본 값을 가지고 있다.
        // 기본 자료형은 메서드를 가지지 않는다.
        // 기본 자료형은 소문자로 시작한다.

        // 참조 자료형 : String, Camera, FactoryCam, SpeedCam ...
        // 참조 자료형의 경우 따로 값을 지정하지 않을 경우 null 이다.
        // 참조 자료형은 메서드를 가질 수 있다.
        // 참조 자료형은 대문자로 시작한다.


    }
}
